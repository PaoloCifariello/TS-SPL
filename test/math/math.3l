
var Math = { };

Math.sum = function(a,b) { 
	return a + b;
};

Math.sub = function(a,b) {
	return a - b;
};

Math.pow = function(a,n) {
	var res = 1;
		
	while (n > 0) {
		res = res * a;
		n = n - 1;
	}
	
	return res;
};

print("5 to the 12th");
var b = Math.pow(5,12);
print("Result: " + b);